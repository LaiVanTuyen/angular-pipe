import { TemConverterPipe } from './tem-converter.pipe';

describe('TemConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new TemConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
